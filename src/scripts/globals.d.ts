/**
 * The last ambient global. `app/app.js` assigns the running app instance onto
 * `window` and the views reach it from anywhere. Do not add new entries.
 */

/** Legacy YUIDoc annotation still present in older comments. */
type Integer = number;

/** The running app instance, assigned by `app/app.js`. */
declare const app: any;
